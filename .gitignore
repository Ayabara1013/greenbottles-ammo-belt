# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/

# Node (if you add npm later)
node_modules/


# Compiled LevelDB compendium — built from src/packs/ by the release workflow
# (and locally via the Foundry CLI). Source of truth is src/packs/, not this.
/packs/

# One-off transform helper
build-src.mjs